server:  
  port: ${PORT:2222}

eureka:  
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

cloud:  
  sadr:
    name: sadr-service
    pingUrl: http://${cloud.sadr.name}/sadr/ping/
